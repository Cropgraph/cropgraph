node_modules/
dist/
*.log
*.tgz
.DS_Store
.env
.env.*
!.env.example
coverage/

# Per-user AI workflow stays local — never commit.
.agents/
.claude/
CLAUDE.md

# Curation scratch dirs (one-shot scripts and intermediate JSON for data
# audits like the pest-species GBIF match). Kept local for reproducibility,
# never shipped.
.scratch-*/
